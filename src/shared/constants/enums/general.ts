export enum Categories {
  PersonalDetails = 'PersonalDetails',
  Investments = 'Investments',
  Insurance = 'Insurance',
  Review = 'Review',
}

export enum CategoriesSection {
  PersonalDetails = 1,
  Investments,
  Insurance,
  Review,
}

export enum AemApiUrl {
  MemberJoinForm = 'memberJoinForm.model.json',
}
